import styled from 'styled-components';
import { BorderedCard } from '../../common/components/Card';

export const ResultItem = styled(BorderedCard)`
  font-size: 20px;
  margin: 16px 0;
  padding: 4px 8px;
`;
